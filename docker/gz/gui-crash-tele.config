<?xml version="1.0"?>
<!--
  Minimal Gazebo GUI for recording the demo flight.

  The stock gui.config docks an Entity Tree and a Component Inspector that
  together occupy about half the window, and the default camera sits at ground
  level so the aircraft is never in frame. This config keeps only what is needed
  to render and follow the vehicle:

    MinimalScene    the 3D view itself
    GzSceneManager  keeps the scene in sync with the server
    CameraTracking  provides /gui/follow, used by record_demo.sh

  Everything else - toolbars, panels, spawn/transform widgets - is dropped, so
  the whole window is render output.
-->
<window>
  <width>1280</width>
  <height>800</height>
  <menus>
    <drawer default="false"/>
  </menus>
</window>

<plugin filename="MinimalScene" name="3D View">
  <gz-gui>
    <title>3D View</title>
    <property type="bool" key="showTitleBar">false</property>
    <property type="string" key="state">docked</property>
  </gz-gui>
  <engine>ogre2</engine>
  <scene>scene</scene>
  <ambient_light>0.4 0.4 0.4</ambient_light>
  <background_color>0.8 0.8 0.8</background_color>
  <!--
    Telephoto shot for the motor-failure run. A camera close enough to render a
    recognisable airframe cannot also contain the cruise and the fall, so this
    stays back at 26 m and narrows the lens instead: 0.40 rad (~23 deg) against
    the ~60 deg default is roughly 2.6x magnification.

    Pair with CAM_MODE=look_at, which keeps the aircraft centred as it tumbles —
    essential with a narrow lens, since a telephoto loses a moving subject fast.
  -->
  <camera_pose>18 -26 10 0 0.10 1.5708</camera_pose>
  <horizontal_fov>0.40</horizontal_fov>
</plugin>

<plugin filename="GzSceneManager" name="Scene Manager">
  <gz-gui>
    <property key="resizable" type="bool">false</property>
    <property key="width" type="double">5</property>
    <property key="height" type="double">5</property>
    <property key="state" type="string">floating</property>
    <property key="showTitleBar" type="bool">false</property>
  </gz-gui>
</plugin>

<plugin filename="CameraTracking" name="Camera Tracking">
  <gz-gui>
    <property key="resizable" type="bool">false</property>
    <property key="width" type="double">5</property>
    <property key="height" type="double">5</property>
    <property key="state" type="string">floating</property>
    <property key="showTitleBar" type="bool">false</property>
  </gz-gui>
</plugin>
